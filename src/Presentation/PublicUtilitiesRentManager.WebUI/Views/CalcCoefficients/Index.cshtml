@model IEnumerable<CalcCoefficientViewModel>

@{
    ViewData["Title"] = "Коэффициенты расчётов";
}

<h1>Коэффициенты расчётов</h1>

@if (User.IsInRole("Administrator"))
{
    <p>
        <a asp-action="Create">Создать</a>
    </p>
}
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Condition)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Coefficient)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Condition)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Coefficient)
                </td>
                <td>
                    @if (User.IsInRole("Administrator"))
                    {
                        @Html.ActionLink("Редактировать", "Edit", new { id = item.Condition }) <span>|</span>
                    }
                    @Html.ActionLink("Подробнее", "Details", new { id = item.Condition })
                    @if (User.IsInRole("Administrator"))
                    {
                        <span>|</span> @Html.ActionLink("Удалить", "Delete", new { id = item.Condition })
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
